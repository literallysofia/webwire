export interface IRectangle {
  x: number;
  y: number;
  height: number;
  width: number;
}

export interface TextProps {
  align: string;
  content: string;
  fsize: number;
  lheight: number;
}

export interface Website {
  id: number;
  url: string;
}
